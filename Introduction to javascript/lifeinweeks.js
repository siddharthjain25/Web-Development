function lifeInWeeks(age) {
    
    /************Don't change the code above************/    
    var days = 365 * (90 - age);
    console.log(days);
    var weeks = 52 * (90 - age);
    console.log(weeks);
    var months = 12 * (90 - age);
    console.log(months);
    /*************Don't change the code below**********/
    }

lifeInWeeks(12);